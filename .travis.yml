language: python

install:
  - pip install python-coveralls

script: python setup.py test

after_success:
  - pip install -r requirements-testing.txt -e .
  - py.test --cov=poultry --cov-report=term-missing test
  - coveralls
